<div class="container" style="padding:60px 24px 100px; max-width:760px;">
  <h1>{{ page.title }}</h1>
  <div style="color:var(--color-gray-dark); line-height:1.7;">
    {{ page.content }}
  </div>
</div>
